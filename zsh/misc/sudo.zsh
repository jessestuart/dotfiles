#!/usr/bin/env zsh

# setopt zle
# function insert_sudo () {
#   if [[ "$BUFFER" != su(do|)\ * ]]; then
#     BUFFER="sudo $BUFFER"
#     (( CURSOR += 5 ))
#   fi
# }

# zle -N insert-sudo insert_sudo
# bindkey "^X^S" insert-sudo
